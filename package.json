{
  "name": "@lidprotocol/xlock-contracts",
  "version": "1.0.1",
  "author": "lidprotocol",
  "license": "GPL-3.0-only",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx hardhat compile",
    "deploy-ropsten": "npx hardhat run --network ropsten scripts/deploy.js",
    "deploy-mainnet": "npx hardhat run --network mainnet scripts/deploy.js",
    "upgrade-ropsten": "npx hardhat run --network ropsten scripts/upgrade.js",
    "upgrade-mainnet": "npx hardhat run --network mainnet scripts/upgrade.js"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.1",
    "@nomiclabs/hardhat-etherscan": "^2.1.0",
    "@openzeppelin/contracts-ethereum-package": "^3.0.0",
    "@openzeppelin/hardhat-upgrades": "^1.4.2",
    "ethers": "^5.0.23",
    "hardhat": "^2.0.5"
  },
  "dependencies": {
    "load-json-file": "^6.2.0"
  }
}
